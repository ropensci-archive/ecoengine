
#'sensor aggregation
#'
#' Aggregated sensor data
#' @template pages
#' @template dates
#' @template foptions
#' @export
#' @examples \dontrun{
#' ee_sensor_agg(min = "1994-04-04", max="2001-01-01")
#'}
ee_sensor_agg <- function(page = NULL, page_size = 25, min_date = NULL, max_date = NULL, foptions = list()) {
	# to be coded.
}